export type Position = {
    id: number;          // Corresponds to Integer id in Java
    variantId: number;    // Corresponds to Integer variantId in Java
    amount: number;       // Corresponds to Integer amount in Java
    eventId: number;      // Corresponds to Integer eventId in Java
}